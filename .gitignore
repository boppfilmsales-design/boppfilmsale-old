# 第三方 / 运行时生成 / 冗余，不纳入版本库
aspnet_client/
HttpErrors/
huiguerdata/
yuzhiguoeditor/

# 本地排查/临时文件（非源码）
staged.txt
push.log
tracked.txt
commit.log

# 图片资源（暂不上传，后续走对象存储/CDN）
# 如需恢复：从下方移除对应行，再 git add -f 强制加入
images/
pic/

# 操作系统 / 编辑器文件
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# 敏感配置（如有数据库密码等，按需添加）
# *.mdb
# *.config
